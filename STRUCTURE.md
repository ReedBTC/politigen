# Politigen Repository Structure

```
politigen/
│
├── README.md                    # Project overview and quick-start
├── STRUCTURE.md                 # This file
├── requirements.txt             # Python dependencies
├── .gitignore
│
├── collect_politigen.py         # Single data collection script.
│                                # Downloads from GitHub & BLS, writes all CSVs to data/.
│                                # Run from repo root: python collect_politigen.py
│
├── data/                        # All datasets (CSV). Auto-generated by collect_politigen.py.
│   │                            # Committed to the repo so the site works without running
│   │                            # the script first. Refresh whenever you want fresh data.
│   │
│   ├── congress_historical.csv       # One row per Congress (57th–119th, 1901–2025).
│   │                                 # Columns: congress, year, total_members,
│   │                                 #          mean_age, median_age,
│   │                                 #          senate_mean_age, house_mean_age,
│   │                                 #          pct_progressive_generation,
│   │                                 #          pct_missionary_generation,
│   │                                 #          pct_lost_generation,
│   │                                 #          pct_gi_generation,
│   │                                 #          pct_silent_generation,
│   │                                 #          pct_baby_boom_generation,
│   │                                 #          pct_gen_x_13ers,
│   │                                 #          pct_millennial_generation,
│   │                                 #          senate_pct_* (same cols for Senate only)
│   │                                 # Feeds: Section 1 (Rise & Fall) and
│   │                                 #        Section 2 (Age Trend)
│   │
│   ├── congress_snapshots_detail.csv # Per-generation stats at 4 moments in time.
│   │                                 # Columns: year, chamber, generation, gen_key,
│   │                                 #          n, mean_age, pct
│   │                                 # Years: 1965, 1985, 2005, 2025
│   │                                 # Chambers: Full Congress, Senate
│   │                                 # Generations: Progressive, Missionary, Lost (where
│   │                                 #   present), G.I., Silent, Boomer, Gen X, Millennial
│   │                                 # Feeds: Section 3 (Snapshots)
│   │
│   └── bls_gen_comparison.csv        # Generational share % by industry sector
│                                     # plus an Elected Officials row.
│                                     # Columns: sector, is_elected, is_all_workers,
│                                     #          pct_under_25, pct_25_44,
│                                     #          pct_45_64, pct_65_plus,
│                                     #          pct_genz, pct_millennial,
│                                     #          pct_genx, pct_boomer, pct_silent
│                                     # Feeds: Section 0 (Workforce Comparison)
│
└── index.html                   # The visualization. One self-contained file.
                                 # Served via GitHub Pages from repo root.
                                 # Loads all data from data/ CSVs at runtime via fetch().
                                 # No build step — open index.html or deploy to any
                                 # static host and it works.
```

## Quick Start

```bash
# Clone and enter the repo
git clone https://github.com/ReedBTC/politigen.git
cd politigen

# Install dependencies
pip install -r requirements.txt

# Refresh all data (optional — CSVs are already committed)
python collect_politigen.py

# View the site locally via any static server, e.g.:
python -m http.server 8000
# then open http://localhost:8000
# (opening index.html as a file:// URL won't work — fetch() is blocked by browsers)
```

## Data Sources

| Source | What it provides |
|--------|-----------------|
| [unitedstates/congress-legislators](https://github.com/unitedstates/congress-legislators) | Every Congress member since 1789, with exact birth dates and term dates |
| [BLS CPS Table 18b](https://www.bls.gov/cps/cpsaat18b.xlsx) | Employed persons by industry and age bracket (annual average) |

The legislators source is fetched as two JSON files: `legislators-historical.json` (~15MB) and `legislators-current.json`. Both are downloaded in a single run of `collect_politigen.py` and are not stored in the repo.

## Generational Framework

All generation assignments use **Strauss-Howe** cutoffs:

| Generation            | Birth Years  |
|-----------------------|--------------|
| Pre-G.I. (fallback)   | before 1843  |
| Progressive           | 1843–1859    |
| Missionary            | 1860–1882    |
| Lost                  | 1883–1900    |
| G.I. Generation       | 1901–1924    |
| Silent                | 1925–1942    |
| Baby Boom             | 1943–1960    |
| Gen X (13ers)         | 1961–1981    |
| Millennial            | 1982–2005    |
| Gen Z                 | 2006+        |

Congressional data uses **exact birth dates** from the legislators JSON. BLS data uses age brackets, so generational shares are **approximated** by proportionally splitting BLS's 10-year brackets across generation boundaries.

## Data Flow

```
unitedstates/congress-legislators (GitHub)
    └── legislators-historical.json  ─┐
    └── legislators-current.json     ─┴─► collect_politigen.py ──► data/congress_historical.csv
                                                                 ──► data/congress_snapshots_detail.csv
                                                                 ──► data/bls_gen_comparison.csv
BLS.gov (CPS Table 18b Excel)
    └── cpsaat18b.xlsx ───────────────────► collect_politigen.py ──► data/bls_gen_comparison.csv

data/*.csv ───────────────────────────────► index.html (fetched at runtime via fetch())
```

## What Was Removed

The original repo had `scripts/`, `charts/`, and `docs/` folders containing multiple
collection scripts, matplotlib chart generators, and several standalone HTML chart files.
All of that has been consolidated:

- 5 Python scripts → 1 (`collect_politigen.py`)
- Multiple chart HTML files → 1 (`index.html`, served from repo root via GitHub Pages)
- Intermediate/derived CSVs → removed (only final CSVs kept in `data/`)
- `docs/` (GitHub Pages) → removed (GitHub Pages now serves from repo root)
- Hardcoded JS data arrays → removed (all data loaded from CSVs at runtime)
